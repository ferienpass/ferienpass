<div class="rounded-md bg-green-50 dark:bg-green-900 dark:bg-opacity-50 p-4">
    <div class="flex">
        <div class="flex-shrink-0">
            <span class="block h-5 w-5 text-green-500 dark:text-green-300">{{ source('@FerienpassAdmin/icons/check-circle.solid.svg') }}</span>
        </div>
        <p class="ml-2 text-sm leading-5 text-green-700 dark:text-green-200">{% block message %}{{ message }}{% endblock %}</p>
    </div>
</div>
