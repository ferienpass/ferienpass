{% extends "@FerienpassAdmin/layout/base.html.twig" %}

{% block main %}

    <div class="px-6 py-4">
        {{ form_start(form) }}
        <div class="space-y-6">
            {{ form_rest(form) }}
        </div>
        {{ form_end(form) }}
    </div>

{% endblock %}
