{% extends "@FerienpassAdmin/layout/login.html.twig" %}

{% block content %}


    <p class="mt-2 text-sm leading-5 text-gray-600 _dark:text-gray-200 max-w">Geben Sie Ihre hinterlegte
        E-Mail-Adresse ein, wenn Sie Ihr Passwort zurücksetzen wollen.</p>

    <div class="mt-8">

        {{ form_start(form) }}
        <div class="space-y-6">
            {% for widget in form %}
                {{ form_row(widget) }}
            {% endfor %}
        </div>
        {{ form_end(form) }}

    </div>

    <div class="mt-6">
        {% include '@FerienpassAdmin/components/back-link.html.twig' with {uri: '/'} %}
    </div>

{% endblock %}