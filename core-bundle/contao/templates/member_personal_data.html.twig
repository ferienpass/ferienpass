<form id="{{ formId }}" method="post" enctype="{{ enctype }}">
    <input type="hidden" name="FORM_SUBMIT" value="{{ formId }}">
    <input type="hidden" name="REQUEST_TOKEN" value="{{ contao_request_token() }}">
        <div>
            <h2 class="text-lg leading-6 font-medium text-gray-900">Persönliche Daten</h2>
            <p class="mt-1 text-sm text-gray-500">Hinterlegen Sie eine aktuelle Telefon- und Handynummer, damit die
                Veranstalter:innen Sie
                kontaktieren können.</p>
        </div>
        <div class="mt-6">

            {% if message is defined %}
                <p class="tl_confirm">{{ message }}</p>
            {% endif %}

            <div class="grid sm:grid-cols-2 gap-6">
                {{ fields|raw }}
            </div>
        </div>
        <div class="mt-8 flex justify-end">
            <span class="ml-3 inline-flex rounded-md shadow-sm">
                <button type="submit"
                        class="inline-flex justify-center py-2 px-4 border border-transparent text-sm leading-5 font-medium rounded-md text-white bg-primary-600 hover:bg-primary-500 focus:outline-none focus:border-primary-700 focus:shadow-outline-primary active:bg-primary-700 transition duration-150 ease-in-out">{{ slabel }}</button>
            </span>
    </div>
</form>
