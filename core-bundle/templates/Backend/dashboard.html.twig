{% extends "@FerienpassCore/Backend/be_page.html.twig" %}

{% block headline %}
    {{ 'MSC.dashboard'|trans([], 'contao_default') }}
{% endblock %}

{% block main %}
    <div class="px-8 py-4 space-y-10">

        <div class="">
            <h1 class="text-gray-900 tracking-tight text-2xl font-extrabold">Hi {{ app.user.name|split(' ')|first~'!' }}</h1>
            <div class="mt-3 max-w-2xl text-xl leading-7 text-gray-600 dark:text-gray-400 sm:mt-4">Was gibt es heute zu
                erledigen?
            </div>
        </div>

        {{ dashboard|raw }}

    </div>
{% endblock %}
